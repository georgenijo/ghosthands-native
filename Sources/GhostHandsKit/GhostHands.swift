// GhostHandsKit — placeholder until AXorcist's real API is verified, then the
// hands core (resolve → find → act → verify) lands here.

public enum GhostHands {
    public static let version = "0.8.17-m4"
}
